export default function HowItWorksPage() {
  return (
    <main className="site-shell">
      <section className="panel">
        <p className="eyebrow">How it works</p>
        <h1>Voice request to outfit plan.</h1>
        <p className="lede">The app reads intent, context, closet data, and constraints before returning a structured recommendation.</p>
      </section>
    </main>
  );
}
